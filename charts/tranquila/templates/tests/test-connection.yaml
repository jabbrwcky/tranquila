apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tranquila.fullname" . }}-test-connection"
  labels:
    {{- include "tranquila.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tranquila.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
