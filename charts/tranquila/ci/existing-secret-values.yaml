# Credentials and config managed out of band. Exercises the branches where the
# chart renders neither its own Secret nor its own ConfigMap, and mounts the
# user's instead.
existingSecret: tranquila-s3-credentials
existingConfig: tranquila-sync-config
