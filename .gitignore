# The built binary, anchored to the repo root: unanchored, this also matched
# charts/tranquila/ and silently excluded the whole chart.
/tranquila
.envrc

# Go
*.o
*.a
*.so
.DS_Store
/dist/
/vendor/
*.rdb

# helm-ls cache
.helm_ls_cache/
